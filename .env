# Guest Manager — Local Development Environment
# ===============================================
# These values are safe defaults for local development.
# For production, use the project's secret manager (never commit real secrets).

# ---------------------------------------------------------------------------
# PUBLIC (Client-Side)
# ---------------------------------------------------------------------------
VITE_APP_NAME=Guest Manager
VITE_API_BASE_URL=
VITE_AUTH_ENABLED=true

# ---------------------------------------------------------------------------
# SERVER-ONLY
# ---------------------------------------------------------------------------
# (Add server secrets via Lovable Secrets — do not paste them here)
